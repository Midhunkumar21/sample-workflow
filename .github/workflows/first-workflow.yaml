name: my first workflows

on: push

jobs:
  first_jobs:
    runs-on: ubuntu-latest
    steps:
    - name: welcome workflow
      run: echo "my first workflow"

    - name: read file
      run: cat README.md

          
         