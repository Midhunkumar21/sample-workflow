name: my first workflows

on: push

jobs:
  first_jobs:
    runs-on: ubuntu-latest
    steps:

    -name: checkout repo
     run: action/checkout@v1

    - name: welcome workflow
      run: echo "my first workflow"

    - name: list file
      run: ls

  


          
         
